function calcular() {
    let ip = Number(document.getElementById('icompra').value)
    let r = document.getElementById('res')

    if (ip >= 100 ) {
        100 * 0.10 == 100
        100 - 
        r.innerText = (`${ip}`)
    }
}