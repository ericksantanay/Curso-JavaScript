function verif() {
    // Variaveis como o input e o paragrafo que vai sair o resultado
    let ip = Number(document.getElementById('').value)

    // Variavel com a senha que eu escolhi
    const senha = 8
}